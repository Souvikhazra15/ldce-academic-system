# 🏗️ C2C System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND                                 │
│                    (React + Vite)                                │
│                   Port: 5173                                     │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ HTTP/REST API
                         │ JWT Authentication
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                      BACKEND SERVER                              │
│                    (Node.js + Express)                           │
│                       Port: 5000                                 │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Auth Layer                            │  │
│  │  • JWT Token Generation & Verification                   │  │
│  │  • Password Hashing (bcrypt)                             │  │
│  │  • Role-Based Access Control                             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                  API Routes                              │  │
│  │  • /api/auth/* - Authentication                          │  │
│  │  • /api/courses/* - Course Management (TODO)             │  │
│  │  • /api/assessments/* - Assessment Management (TODO)     │  │
│  │  • /api/attendance/* - Attendance Tracking (TODO)        │  │
│  │  • /api/reports/* - Report Generation (TODO)             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                 Business Logic                           │  │
│  │  • User Management                                       │  │
│  │  • Course Outcome Calculation                            │  │
│  │  • CO-PO Attainment                                      │  │
│  │  • Attendance Analytics                                  │  │
│  │  • Report Generation                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   Prisma ORM                             │  │
│  │  • Type-safe database queries                            │  │
│  │  • Automatic migrations                                  │  │
│  │  • Relationship management                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         │ SQL Queries
                         │
┌────────────────────────▼────────────────────────────────────────┐
│                    PostgreSQL Database                           │
│                  (Prisma Cloud / Local)                          │
│                      Port: 5432                                  │
│                                                                  │
│  18 Tables:                                                      │
│  • users, faculty_profiles, student_profiles                    │
│  • departments, programs, academic_terms                         │
│  • divisions, student_batches                                    │
│  • subjects, curriculum_mappings                                 │
│  • course_outcomes, co_po_matrices                              │
│  • assessments, assessment_configs, student_marks               │
│  • attendance_sessions, attendance_records                       │
│  • lecture_plans                                                │
└──────────────────────────────────────────────────────────────────┘
```

## Authentication Flow

```
┌──────────┐                                      ┌──────────┐
│          │  1. POST /api/auth/login            │          │
│  Client  ├────────────────────────────────────►│  Server  │
│          │  { email, password }                │          │
└──────────┘                                      └─────┬────┘
                                                        │
                                                        │ 2. Verify
                                                        │    password
                                                        │
                                                  ┌─────▼────┐
                                                  │ Database │
                                                  └─────┬────┘
                                                        │
┌──────────┐                                      ┌─────┴────┐
│          │  3. Return tokens                    │          │
│  Client  │◄────────────────────────────────────┤  Server  │
│          │  { accessToken, refreshToken }      │          │
└────┬─────┘                                      └──────────┘
     │
     │ 4. Store tokens
     │
┌────▼─────┐
│ Storage  │
│ (Memory/ │
│ Storage) │
└──────────┘

For Protected Requests:
┌──────────┐                                      ┌──────────┐
│          │  GET /api/auth/profile              │          │
│  Client  ├────────────────────────────────────►│  Server  │
│          │  Authorization: Bearer <token>      │          │
└──────────┘                                      └─────┬────┘
                                                        │
                                                        │ Verify JWT
                                                        │
┌──────────┐                                      ┌─────┴────┐
│          │  User data with profile              │          │
│  Client  │◄────────────────────────────────────┤  Server  │
│          │                                      │          │
└──────────┘                                      └──────────┘
```

## Database Relationships

```
Department ──┬──► Program ──► AcademicTerm ──► Division ──┬──► StudentBatch
             │                                            │
             │                                            └──► AttendanceSession
             │
             ├──► FacultyProfile ──► User
             │
             └──► Subject ──┬──► CourseOutcome ──┬──► COPOMatrix
                            │                     │
                            │                     └──► AssessmentConfig ──► StudentMark
                            │
                            └──► LecturePlan ──► AttendanceSession


User Hierarchy:
User (id, email, password, role)
  ├──► FacultyProfile (departmentId, designation)
  └──► StudentProfile (enrollmentNo, divisionId, batchId)
```

## User Roles & Access

```
                         ┌─────────────┐
                         │    ADMIN    │
                         │  Full Access│
                         └──────┬──────┘
                                │
                ┌───────────────┼───────────────┐
                │               │               │
         ┌──────▼──────┐ ┌─────▼──────┐ ┌─────▼──────┐
         │     HOD      │ │  FACULTY   │ │  STUDENT   │
         │ Department   │ │  Teaching  │ │   View     │
         │  Management  │ │  & Marks   │ │   Own Data │
         └──────────────┘ └────────────┘ └────────────┘
```

## Data Flow: Assessment Entry

```
1. Faculty Login
   ↓
2. Select Course & Division
   ↓
3. Create Assessment (MSE/ESE/Quiz)
   ↓
4. Configure Questions → CO Mapping
   ↓
5. Enter Student Marks
   ↓
6. System Calculates:
   - CO Attainment
   - PO Attainment (via CO-PO Matrix)
   - Student Performance
   ↓
7. Generate Reports
```

## API Response Structure

```json
Success Response:
{
  "success": true,
  "message": "Operation successful",
  "data": {
    // Response data here
  }
}

Error Response:
{
  "success": false,
  "message": "Error description",
  "error": "Detailed error (dev mode only)"
}
```

## Security Layers

```
┌─────────────────────────────────────────────────────┐
│ Layer 1: HTTPS (Production)                         │
├─────────────────────────────────────────────────────┤
│ Layer 2: CORS (Cross-Origin Protection)             │
├─────────────────────────────────────────────────────┤
│ Layer 3: Helmet (Security Headers)                  │
├─────────────────────────────────────────────────────┤
│ Layer 4: JWT Authentication                         │
├─────────────────────────────────────────────────────┤
│ Layer 5: Role-Based Authorization                   │
├─────────────────────────────────────────────────────┤
│ Layer 6: Input Validation (TODO)                    │
├─────────────────────────────────────────────────────┤
│ Layer 7: Rate Limiting (TODO)                       │
└─────────────────────────────────────────────────────┘
```

## Development Workflow

```
1. Schema Design (Prisma)
   ↓
2. Generate Client (prisma generate)
   ↓
3. Push to Database (prisma db push)
   ↓
4. Seed Test Data (npm run prisma:seed)
   ↓
5. Develop API Endpoints
   ↓
6. Test in Prisma Studio
   ↓
7. Test with Postman/test-api.js
   ↓
8. Integrate with Frontend
```

## Performance Optimization

```
Database Level:
├── Indexes on frequently queried fields
├── UUID primary keys for distribution
├── Proper foreign key relationships
└── Efficient query patterns

Application Level:
├── JWT caching
├── Connection pooling
├── Selective field loading
└── Pagination (TODO)

API Level:
├── Response compression (TODO)
├── Caching headers (TODO)
├── Rate limiting (TODO)
└── Request validation
```

## Monitoring & Logging

```
Development:
├── Console logs (Morgan)
├── Prisma query logs
└── Error stack traces

Production (TODO):
├── Structured logging (Winston)
├── Error tracking (Sentry)
├── Performance monitoring (New Relic)
└── Database monitoring (Prisma Metrics)
```

## Deployment Architecture (Future)

```
┌─────────────────┐
│   Load Balancer │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌──▼────┐
│ App 1 │ │ App 2 │  (Multiple instances)
└───┬───┘ └───┬───┘
    │         │
    └────┬────┘
         │
┌────────▼────────┐
│   PostgreSQL    │
│   (Primary)     │
└────────┬────────┘
         │
┌────────▼────────┐
│   PostgreSQL    │
│   (Replica)     │
└─────────────────┘
```
